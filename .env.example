# Copy to `.env` or `.env.development.local` for Create React App.

# Backend base URL (no trailing slash). Local dev:
REACT_APP_API_URL=http://localhost:5000

# Production builds: set the same variable in your host (e.g. Vercel project → Environment Variables)
# instead of committing a public URL, if you prefer.
